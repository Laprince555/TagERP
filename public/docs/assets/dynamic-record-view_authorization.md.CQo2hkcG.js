import{_ as o,o as t,c as i,a2 as a}from"./chunks/framework.Buc7gXeZ.js";const b=JSON.parse('{"title":"Authorization","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-record-view/authorization.md","filePath":"dynamic-record-view/authorization.md"}'),n={name:"dynamic-record-view/authorization.md"};function d(r,e,c,l,s,h){return t(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="authorization" tabindex="-1">Authorization <a class="header-anchor" href="#authorization" aria-label="Permalink to &quot;Authorization&quot;">​</a></h1><p>Status: <strong>Implemented.</strong> Every authorization hook in the engine, in the order a request actually hits them.</p><ol><li><strong>Record-level</strong> — <code>DynamicRecordView::query()</code>. The only path a record is ever fetched through (<code>RecordResolver::resolve()</code>/<code>resolveFresh()</code>); rows excluded by this query 404 exactly like rows that don&#39;t exist.</li><li><strong>Tab-level</strong> — <code>RecordTab::visible(bool|callable $condition)</code>, receiving the resolved record. <code>RecordSection::authorizedTabs()</code> filters to only visible tabs before anything renders; <code>defaultTabKey()</code>/ <code>normalizeActiveTabKey()</code> only ever land on an authorized tab.</li><li><strong>Content-block-level</strong> — <code>Content::visible(bool|callable $condition)</code>, same shape, checked before a <code>FieldsContent</code>/<code>TableContent</code> renders.</li><li><strong>Field-level</strong> — <code>Field::visible()</code>/<code>Field::hidden()</code>, same shape, per individual field inside a <code>FieldsContent</code>.</li><li><strong>Sub Application-level</strong> — <code>SubApplication::authorization(bool|callable $condition)</code>, which becomes the generated tab&#39;s <code>visible()</code> condition in <code>otherDataSection()</code>.</li><li><strong>Embedded-table-level</strong> — <code>EmbeddedTableContext::resolveEmbeddedContent()</code> re-checks <code>$content-&gt;isVisible($parent)</code> on every embedded-table interaction (not just initial mount), 404ing if it&#39;s since become unauthorized.</li><li><strong>Relationship-mutation-level</strong> — <code>RelationshipActions::linkAuthorization()</code> / <code>unlinkAuthorization(callable(?User $user, Model $parent, Model $candidate): bool)</code>, re-checked inside <code>RelationshipMutator</code>&#39;s locked transaction immediately before the mutation, using the currently authenticated user (<code>auth()-&gt;user()</code>), not a value captured earlier in the request.</li></ol><p>All conditions accept either a plain <code>bool</code> or a <code>callable(mixed $record): bool</code> (field/content/tab/level) or <code>callable(?User $user, Model $parent, Model $candidate): bool</code> (link/unlink level) — never a Gate/Policy class reference is required, though a condition closure is free to call <code>Gate::allows()</code>/<code>$user-&gt;can()</code> internally if the app wants policy-backed authorization.</p><p>There is no separate &quot;can view this record view at all&quot; gate distinct from <code>query()</code> — if an unauthenticated or unauthorized user&#39;s <code>query()</code> excludes every row, every id 404s for them, which is the intended behavior (see <a href="./security">security.md</a>).</p>',5)])])}const p=o(n,[["render",d]]);export{b as __pageData,p as default};
