import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.Buc7gXeZ.js";const u=JSON.parse('{"title":"User Preferences","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-table/preferences.md","filePath":"dynamic-table/preferences.md"}'),n={name:"dynamic-table/preferences.md"};function r(o,e,d,l,c,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="user-preferences" tabindex="-1">User Preferences <a class="header-anchor" href="#user-preferences" aria-label="Permalink to &quot;User Preferences&quot;">​</a></h1><p>Automatic, per-user persistence of column visibility/order and per-page — <strong>not</strong> a saved view (see <a href="./saved-views">saved-views.md</a> for filters/search/sort persistence).</p><h2 id="what-is-persisted-✅-implemented" tabindex="-1">What is persisted (✅ Implemented) <a class="header-anchor" href="#what-is-persisted-✅-implemented" aria-label="Permalink to &quot;What is persisted (✅ Implemented)&quot;">​</a></h2><ul><li>Column visibility (which toggleable columns are hidden)</li><li>Column order</li><li>Per-page value</li><li>Density (<code>compact</code>/<code>comfortable</code>/<code>spacious</code> — stored, not yet rendered differently by the UI)</li><li>Schema version (for stale-preference normalization)</li></ul><h2 id="what-is-not-persisted" tabindex="-1">What is NOT persisted <a class="header-anchor" href="#what-is-not-persisted" aria-label="Permalink to &quot;What is NOT persisted&quot;">​</a></h2><ul><li>Current page</li><li>Search text</li><li>Filter state</li></ul><p>These are temporary, request-scoped state — they live in Livewire&#39;s namespaced query-string params (see <code>Table::queryString()</code>), not in the preferences table.</p><h2 id="database-schema" tabindex="-1">Database schema <a class="header-anchor" href="#database-schema" aria-label="Permalink to &quot;Database schema&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>user_table_preferences</span></span>
<span class="line"><span>- id</span></span>
<span class="line"><span>- user_id           (FK, cascade on delete)</span></span>
<span class="line"><span>- table_key</span></span>
<span class="line"><span>- preferences        JSON  — {version, hidden_columns, column_order, per_page, density}</span></span>
<span class="line"><span>- schema_version     unsigned int</span></span>
<span class="line"><span>- created_at / updated_at</span></span>
<span class="line"><span></span></span>
<span class="line"><span>UNIQUE(user_id, table_key)</span></span>
<span class="line"><span>INDEX(table_key)</span></span></code></pre></div><p><code>App\\Models\\UserTablePreference</code> casts <code>preferences</code> to an array and <code>schema_version</code> to an int.</p><h2 id="storage-contract" tabindex="-1">Storage contract <a class="header-anchor" href="#storage-contract" aria-label="Permalink to &quot;Storage contract&quot;">​</a></h2><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TablePreferenceStore</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Authenticatable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $user, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $tableKey)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Authenticatable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $user, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $tableKey, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $preferences)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Authenticatable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $user, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $tableKey)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Bound to <code>App\\Support\\DynamicTable\\PreferenceStores\\EloquentTablePreferenceStore</code> in <code>AppServiceProvider</code>. Every query is scoped to the given <code>$user</code>&#39;s own id — there is no code path that accepts a client-supplied user id.</p><h2 id="ownership" tabindex="-1">Ownership <a class="header-anchor" href="#ownership" aria-label="Permalink to &quot;Ownership&quot;">​</a></h2><ul><li><code>get</code>/<code>save</code>/<code>delete</code> always filter by <code>$user-&gt;getAuthIdentifier()</code> — a user can only ever read or write their own row.</li><li>Guests (<code>!auth()-&gt;check()</code>) never trigger a read or write; the table falls back to definition defaults for the request.</li></ul><h2 id="concurrency" tabindex="-1">Concurrency <a class="header-anchor" href="#concurrency" aria-label="Permalink to &quot;Concurrency&quot;">​</a></h2><p><code>save()</code> uses <code>upsert(..., uniqueBy: [&#39;user_id&#39;, &#39;table_key&#39;])</code>, which relies on the table&#39;s own unique constraint — two concurrent saves for the same user/table resolve to one row via the database&#39;s own conflict resolution, never a duplicate-row race.</p><h2 id="load-once-guarantee" tabindex="-1">Load-once guarantee <a class="header-anchor" href="#load-once-guarantee" aria-label="Permalink to &quot;Load-once guarantee&quot;">​</a></h2><p>Preferences are read exactly once, in <code>Table::mount()</code> — never re-queried during <code>render()</code>. Verified by <code>tests/Feature/DynamicTable/PreferencesTest.php</code>&#39;s <code>&#39;preferences load once during mount and are not re-queried on subsequent renders&#39;</code> test (asserts zero <code>user_table_preferences</code> queries after the initial mount).</p><h2 id="reset-to-default" tabindex="-1">Reset to default <a class="header-anchor" href="#reset-to-default" aria-label="Permalink to &quot;Reset to default&quot;">​</a></h2><p><code>Table::resetPreferences()</code> deletes the stored row and re-derives <code>TablePreferences::normalize()</code> against the live table definition (i.e. defaults) — one delete query, no leftover state.</p><h2 id="schema-versioning-stale-preference-normalization" tabindex="-1">Schema versioning &amp; stale-preference normalization <a class="header-anchor" href="#schema-versioning-stale-preference-normalization" aria-label="Permalink to &quot;Schema versioning &amp; stale-preference normalization&quot;">​</a></h2><p><code>TablePreferences::normalize(TableDefinition $definition, ?array $raw)</code> is the single point every stored (or absent) preference payload passes through before being applied:</p><table tabindex="0"><thead><tr><th>Scenario</th><th>Behavior</th></tr></thead><tbody><tr><td>No stored row (<code>$raw === null</code>)</td><td>Seeds <code>hidden_columns</code> from every <code>-&gt;hiddenByDefault()</code> column</td></tr><tr><td>A column was added to the definition since last save</td><td>Appended to <code>column_order</code> in definition order, visible by default</td></tr><tr><td>A column was removed from the definition</td><td>Dropped from both <code>hidden_columns</code> and <code>column_order</code></td></tr><tr><td>A column&#39;s <code>visible()</code> now returns <code>false</code></td><td>Dropped from both, exactly like a removed column</td></tr><tr><td>Duplicate keys in stored <code>column_order</code></td><td>Deduplicated</td></tr><tr><td>A fixed (<code>toggleable(false)</code>) column</td><td>Always kept in its defined relative position, never in <code>hidden_columns</code></td></tr><tr><td><code>per_page</code> outside the allowlist</td><td>Clamped to the allowlist</td></tr><tr><td>Invalid <code>density</code> value</td><td>Falls back to <code>&#39;comfortable&#39;</code></td></tr><tr><td>Any <code>schema_version</code></td><td>Bumped to the current version (<code>1</code>) after normalization</td></tr></tbody></table><p>This means a stale preferences row from before a table definition changed can never crash rendering or leak a since-removed/unauthorized column — it&#39;s silently repaired on next load.</p>`,25)])])}const k=a(n,[["render",r]]);export{u as __pageData,k as default};
