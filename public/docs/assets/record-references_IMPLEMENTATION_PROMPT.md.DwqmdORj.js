import{_ as i,o as a,c as t,a2 as o}from"./chunks/framework.Buc7gXeZ.js";const m=JSON.parse('{"title":"Copy-ready implementation prompt: Record References","description":"","frontmatter":{},"headers":[],"relativePath":"record-references/IMPLEMENTATION_PROMPT.md","filePath":"record-references/IMPLEMENTATION_PROMPT.md"}'),r={name:"record-references/IMPLEMENTATION_PROMPT.md"};function n(l,e,d,c,s,p){return a(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="copy-ready-implementation-prompt-record-references" tabindex="-1">Copy-ready implementation prompt: Record References <a class="header-anchor" href="#copy-ready-implementation-prompt-record-references" aria-label="Permalink to &quot;Copy-ready implementation prompt: Record References&quot;">​</a></h1><p>Implement the Record References system in <code>D:\\projects\\TagERP</code> completely and verify it. Do not stop at a proposal. Preserve unrelated work and never overwrite user changes. Follow <code>AGENTS.md</code>, <code>CLAUDE.md</code>, <code>.ai/rules/index.md</code>, every path-matching project rule, and all relevant Laravel, Livewire, Flux UI, Tailwind CSS, and Pest skills. Before changing version-sensitive code, confirm installed package versions and search the version-specific documentation required by the project.</p><h2 id="outcome" tabindex="-1">Outcome <a class="header-anchor" href="#outcome" aria-label="Permalink to &quot;Outcome&quot;">​</a></h2><p>Build one shared, secure, high-performance way to present any Application record in three variants:</p><ol><li><code>card</code>: Application name/color/icon, record title, and important facts visible immediately.</li><li><code>tag</code>: Application icon/color and record title; primary click opens the record, while a deliberate context-preview action loads and displays important facts in a compact anchored popover.</li><li><code>icon</code>: Application icon/color only; primary click opens the record, while keyboard focus, touch details, or a delayed stable hover loads and displays the record title and important facts.</li></ol><p>All variants must use the same Application-owned provider definition for the record title, facts, route, query requirements, and authorization. Do not duplicate these definitions in views, Dynamic Table columns, or future Dynamic Form fields.</p><p>Read and keep <code>docs/record-references/README.md</code>, <code>docs/dynamic-table/record-references.md</code>, and <code>docs/dynamic-form/record-references.md</code> accurate as implementation progresses.</p><h2 id="repository-facts-and-scope-boundary" tabindex="-1">Repository facts and scope boundary <a class="header-anchor" href="#repository-facts-and-scope-boundary" aria-label="Permalink to &quot;Repository facts and scope boundary&quot;">​</a></h2><ul><li>Application metadata lives in <code>Modules/General/System/Application.php</code> and the original create-table migration lives in <code>Modules/General/Database/Migrations/2026_07_30_160000_create_applications_table.php</code>.</li><li>Dynamic Table exists in <code>app/Support/DynamicTable</code>, <code>app/Livewire/DynamicTable</code>, and corresponding Blade components/tests.</li><li>A shared Dynamic Form engine does not exist at the time this prompt was written. Confirm this again. If it is still absent, do not invent or fake an entire Dynamic Form framework. Deliver a stable, framework-agnostic renderer/provider API and keep the documented <code>RecordReferenceField</code> contract and integration plan accurate. If a real Dynamic Form engine now exists, integrate a typed field and test it using its established conventions.</li><li>Shared cross-module contracts, DTOs, registry, palette resolver, rendering infrastructure, and preview host belong in root <code>app/</code>/<code>resources</code>. Each concrete Application provider stays inside its owning Nwidart module and registers through an explicit public boundary. Do not create <code>Apps/</code> directories or new packages.</li><li>Do not add dependencies without explicit user approval.</li></ul><h2 id="mandatory-application-color-change" tabindex="-1">Mandatory Application color change <a class="header-anchor" href="#mandatory-application-color-change" aria-label="Permalink to &quot;Mandatory Application color change&quot;">​</a></h2><p>The project is still under development. Modify the original applications create-table migration; do not create an <code>add_color_to_applications</code> migration.</p><ul><li>Add a required <code>string(&#39;color&#39;, 32)</code> column with no index and no silent fallback that hides missing seed data.</li><li>Add <code>color</code> to the Application model&#39;s fillable attributes and use a backed enum/cast or an equivalent single trusted allowlist if consistent with project conventions.</li><li>Define a semantic palette that supports at least <code>sky</code>, <code>indigo</code>, <code>violet</code>, <code>amber</code>, <code>emerald</code>, <code>rose</code>, <code>cyan</code>, <code>orange</code>, and <code>slate</code>.</li><li>Locate every Application seeder, factory, fixture, raw insert, and upsert. Add an explicit valid color to every Application. Add <code>color</code> to every <code>upsert()</code> update list so rerunning a seeder can correct it.</li><li>Update all tests and add a regression test proving every seeded Application has a non-null valid color and repeated seeding remains idempotent.</li><li>For current World Applications, use this mapping unless an existing product decision contradicts it: countries <code>sky</code>, states <code>indigo</code>, cities <code>violet</code>, time zones <code>amber</code>, currencies <code>emerald</code>, languages <code>rose</code>, companies <code>cyan</code>, people <code>orange</code>.</li><li>Do not store or render arbitrary Tailwind classes, CSS strings, or unchecked hex values. Resolve the token through a fixed trusted palette into foreground/background/border/focus/dark-mode variables. Never interpolate request/database text into a class name or raw style declaration.</li></ul><p>Because the original migration changes, use the project&#39;s safe development reset/test database flow for verification. Do not perform destructive database reset operations against an unverified or non-test database.</p><h2 id="required-architecture" tabindex="-1">Required architecture <a class="header-anchor" href="#required-architecture" aria-label="Permalink to &quot;Required architecture&quot;">​</a></h2><p>Create small, typed, independently testable concepts equivalent to:</p><ul><li><code>RecordReferenceVariant</code> enum: <code>Card</code>, <code>Tag</code>, <code>Icon</code>.</li><li><code>ApplicationColor</code> enum or one canonical palette allowlist.</li><li><code>RecordFact</code>: escaped label/value with priority and optional safe presentation metadata.</li><li><code>RecordReferenceIdentity</code>: minimal initial payload.</li><li><code>RecordReferencePreview</code>: on-demand facts/status payload.</li><li><code>RecordReferenceProvider</code> contract plus an abstract base only if it removes real duplication.</li><li><code>RecordReferenceRegistry</code>: immutable Application code to trusted provider mapping.</li><li><code>RecordReferencePalette</code>: token to trusted design values.</li><li>Stateless Blade components for the three visual variants.</li><li>Exactly one lazy <code>RecordReferencePreviewHost</code> per page or parent Livewire boundary.</li></ul><p>Do not make one Livewire component per record, table row, cell, card, tag, or icon. Do not put Eloquent models, builders, providers, closures, or column objects in public Livewire state. Do not query in Blade, accessors called by Blade, DTO constructors, fact formatters, or rendering providers.</p><p>Each concrete provider must own and explicitly declare:</p><ul><li>immutable Application code;</li><li>fixed supported model class;</li><li>record title construction;</li><li>server-generated named record route;</li><li>ordered allowlisted facts;</li><li>minimal identity columns/relations;</li><li>additional immediately visible card columns/relations;</li><li>separate preview-only columns/relations;</li><li>policy/gate, tenant, parent, active, and soft-delete scoping;</li><li>optional cache policy, defaulting to no cross-request record caching.</li></ul><p>The client may submit only an allowlisted Application code, bounded scalar record key, and normalized record version. It must never submit a PHP/model/provider class, route name, relation path, database column, SQL expression, template/view name, Tailwind class, or arbitrary color.</p><p>Implement Country (<code>gen-wld-ctr</code>) first as a vertical proof of concept. Complete and verify all three variants and integrations before rolling providers out to additional Applications.</p><h2 id="lazy-data-and-request-rules" tabindex="-1">Lazy data and request rules <a class="header-anchor" href="#lazy-data-and-request-rules" aria-label="Permalink to &quot;Lazy data and request rules&quot;">​</a></h2><p>Split initial identity from preview facts.</p><h3 id="card" tabindex="-1">Card <a class="header-anchor" href="#card" aria-label="Permalink to &quot;Card&quot;">​</a></h3><p>Card facts are visible, so fetch only the provider-declared card fields/relations in the same bounded, paginated query that fetched the visible records. There must be no request or query per card and no secondary preview request for already visible facts.</p><h3 id="tag" tabindex="-1">Tag <a class="header-anchor" href="#tag" aria-label="Permalink to &quot;Tag&quot;">​</a></h3><p>Fetch identity only on initial render. Do not fetch preview fields or relations. Load the preview only on right click/context menu, keyboard Context Menu/<code>Shift+F10</code>, or a discoverable touch details action. A primary click follows the authorized record route.</p><h3 id="icon" tabindex="-1">Icon <a class="header-anchor" href="#icon" aria-label="Permalink to &quot;Icon&quot;">​</a></h3><p>Fetch identity only on initial render. Do not fetch preview fields or relations. Load the preview on keyboard focus, touch details, or after a stable hover delay of about 400 ms. Cancel before requesting if the pointer leaves during the delay. A primary click follows the authorized record route.</p><h3 id="shared-preview-host" tabindex="-1">Shared preview host <a class="header-anchor" href="#shared-preview-host" aria-label="Permalink to &quot;Shared preview host&quot;">​</a></h3><ul><li>One host services all record references within its page/Livewire boundary.</li><li>Resolve provider and model exclusively from the server registry.</li><li>Validate input and authorize before returning any record metadata.</li><li>Query only provider-declared columns and fixed relations.</li><li>Return a minimal DTO, never a complete model/relationship payload.</li><li>Deduplicate in-flight requests.</li><li>Cache successful authorized results in page memory using Application code, record key, record version, locale, tenant, and permission/user scope as applicable.</li><li>Reopening the same record/version in the same page lifetime must issue no second network request.</li><li>Do not preload all previews, speculate on adjacent rows, or send hidden facts in initial HTML, Alpine data, Livewire state, or data attributes.</li><li>Cross-request fact caching is disabled by default. If a provider opts in, prove its facts are safe to share within the cache scope, include tenant/locale/version/authorization scope in the key, set a bounded TTL, and implement invalidation.</li><li>Application metadata may be cached/memoized and must reuse/integrate with existing navigation cache invalidation. Never query the applications table once per reference.</li><li>Add client delay/cancellation/deduplication. Add a sensible server-side throttle if the selected transport supports it without weakening Livewire/CSRF conventions.</li></ul><h2 id="performance-acceptance-budgets" tabindex="-1">Performance acceptance budgets <a class="header-anchor" href="#performance-acceptance-budgets" aria-label="Permalink to &quot;Performance acceptance budgets&quot;">​</a></h2><ul><li>Tag/icon initial table render: existing pagination count/data queries plus a constant number of required eager-load queries; zero preview queries.</li><li>Card render: the same bounded constant-query model with only card-visible fields and relations; zero queries per rendered card.</li><li>Application metadata: zero queries on cache hit and at most one bounded cache fill, never per row.</li><li>First preview: one primary record query plus a constant number of declared preview relation queries.</li><li>Same preview/version reopened on the same page: zero HTTP/Livewire requests and zero DB queries.</li><li>A hidden Dynamic Table record-reference column: zero additional selects/eager loads.</li><li>Query count must not grow with the number of rows. Add regression tests comparing small and large page sizes and inspect SQL/selects to prove preview-only data is absent initially.</li></ul><p>Use pagination, select only required columns, include relationship matching keys, merge/deduplicate requirements, and prevent lazy loading in tests/development. Do not use <code>SELECT *</code>, unbounded <code>get()</code>, <code>all()</code>, per-row policy queries, per-row Application queries, or per-row network requests.</p><h2 id="security-acceptance-requirements" tabindex="-1">Security acceptance requirements <a class="header-anchor" href="#security-acceptance-requirements" aria-label="Permalink to &quot;Security acceptance requirements&quot;">​</a></h2><ul><li>Authorize every preview as a server entry point before exposing title, URL, facts, or existence.</li><li>Prevent IDOR with provider-owned fixed-model scoped queries and policies/gates.</li><li>Enforce tenant, parent record, Application permission, inactive state, and soft-delete rules.</li><li>Use named routes and scoped route binding for nested resources; generate URLs server-side.</li><li>Do not derive a record URL by blindly concatenating an ID onto the Application route.</li><li>Explicitly allowlist facts. Never call <code>toArray()</code> on a record for a preview.</li><li>Exclude sensitive values from DOM, Livewire public state, response payloads, caches, logs, errors, and analytics.</li><li>Escape title, labels, and values with Blade. Raw HTML is unsupported unless a later explicit audited feature introduces a safe value type.</li><li>Validate/reject invalid variant, color, code, key type/length, and options.</li><li>Return a generic unavailable/forbidden preview state without partial data leakage.</li><li>Remove/disable the route when the user cannot view the record.</li><li>Add adversarial tests for forged Application codes, keys belonging to another tenant/parent, unauthorized/deleted records, XSS payloads, invalid colors, invalid variants, and request flooding controls.</li></ul><h2 id="ui-and-interaction-requirements" tabindex="-1">UI and interaction requirements <a class="header-anchor" href="#ui-and-interaction-requirements" aria-label="Permalink to &quot;UI and interaction requirements&quot;">​</a></h2><p>Use Flux UI free components where available and stateless Blade/Alpine only for the missing anchored context/hover behavior. Follow existing Tailwind v4 conventions. Support RTL, Arabic/English, dark mode, keyboard, screen readers, touch, reduced motion, long text, and viewport collision.</p><ul><li>Card: restrained Application-colored accent, icon block, small Application name, prominent record title, two-to-four prioritized facts, optional semantic status, valid link semantics, no invalid nested interactive elements.</li><li>Tag: compact icon/title chip; primary click navigates; context preview works by right click, keyboard, and discoverable touch details. Do not rely on right click alone.</li><li>Icon: accessible hit target and label; primary click navigates; delayed hover/focus/touch details opens preview. Do not rely on hover alone.</li><li>Preview surface: skeleton while loading, clear unavailable state, Escape/outside-click close, focus restoration, collision-aware positioning, and no layout shift in the table.</li><li>Status and meaning must never be communicated by color alone. Verify contrast in light/dark themes.</li></ul><h2 id="dynamic-table-integration" tabindex="-1">Dynamic Table integration <a class="header-anchor" href="#dynamic-table-integration" aria-label="Permalink to &quot;Dynamic Table integration&quot;">​</a></h2><p>Implement a typed <code>RecordReferenceColumn</code>; do not return HTML from <code>formatUsing()</code>.</p><p>The column must allow a trusted Application code, trusted relationship/already-loaded record source, variant, explicit scalar search/sort/filter backing fields, and explicit raw export behavior. Teach the query builder/renderer to:</p><ul><li>apply initial requirements only when the column is visible;</li><li>select identity only for tag/icon;</li><li>include declared card facts for card;</li><li>keep tag/icon preview requirements out of the initial table query;</li><li>narrow related selects and include Eloquent owner/foreign keys;</li><li>merge duplicate requirements across columns;</li><li>render through the shared Blade component and one preview host;</li><li>retain stable row/cell <code>wire:key</code> values;</li><li>keep browser state allowlisted and primitives-only.</li></ul><p>The existing Dynamic Table relation loader currently loads full related rows. Do not let the new column use that to preload preview data. Add the targeted narrowing needed for record references and test the generated selects and constant query count. Preserve current behavior for unrelated columns unless a small safe refactor is required.</p><p>Update <code>.ai/dynamic-table/CHECKLIST.md</code> and <code>docs/dynamic-table/record-references.md</code> with delivered paths, examples, query measurements, tests, and remaining limitations.</p><h2 id="dynamic-form-integration" tabindex="-1">Dynamic Form integration <a class="header-anchor" href="#dynamic-form-integration" aria-label="Permalink to &quot;Dynamic Form integration&quot;">​</a></h2><p>Confirm whether Dynamic Form exists when implementing:</p><ul><li>If it exists, add a typed <code>RecordReferenceField</code> supporting all variants and the shared preview host. The submitted/persisted form value remains the intended scalar key; do not dehydrate the DTO, provider, Eloquent model, or preview facts. Treat record presentation separately from relation selection/search. Test create/edit/show, validation rerender, changed selection, deleted/inaccessible relation, authorization, and lazy preview reuse.</li><li>If it does not exist, do not build a fake form engine. Ensure the shared renderer/provider public API can satisfy the documented field contract without Dynamic Table dependencies, keep <code>docs/dynamic-form/record-references.md</code> explicit about status, and provide a concrete future integration checklist.</li></ul><h2 id="tests-and-verification" tabindex="-1">Tests and verification <a class="header-anchor" href="#tests-and-verification" aria-label="Permalink to &quot;Tests and verification&quot;">​</a></h2><p>Use Pest and existing conventions. At minimum add:</p><ul><li>schema/model/factory/all-seeder color tests;</li><li>idempotent Application upsert tests that include color updates;</li><li>provider/registry/DTO/palette unit tests;</li><li>Country card/tag/icon render tests;</li><li>authorization, tenant/parent scope, inactive/deleted/missing record tests;</li><li>XSS and forged input tests;</li><li>route generation and unauthorized-link omission tests;</li><li>Dynamic Table visible/hidden column tests;</li><li>SQL field-selection and fixed query-count regressions;</li><li>proof that tag/icon previews are absent before user action;</li><li>first-preview query/request test and repeat-preview no-request browser test;</li><li>delayed-hover cancellation and in-flight deduplication tests where browser tooling allows;</li><li>RTL/dark/mobile/keyboard/focus/Escape/collision behavior tests;</li><li>Dynamic Form tests only if a real engine exists.</li></ul><p>Run the narrowest relevant tests during development, then all affected suites. Run <code>vendor/bin/pint --dirty --format agent</code> after PHP changes and the full relevant test suite before finishing. Build frontend assets if the implementation changes compiled CSS/JS. Inspect the final diff for unrelated changes and do not modify or commit user-owned unrelated untracked files.</p><h2 id="documentation-and-handoff" tabindex="-1">Documentation and handoff <a class="header-anchor" href="#documentation-and-handoff" aria-label="Permalink to &quot;Documentation and handoff&quot;">​</a></h2><p>Update all three referenced documentation files to match reality. Document:</p><ul><li>final namespaces and public APIs;</li><li>how an Application registers a provider and chooses a color;</li><li>how to define facts and query requirements;</li><li>Blade, Dynamic Table, and actual/future Dynamic Form examples;</li><li>lazy preview lifecycle;</li><li>security boundaries and cache rules;</li><li>measured initial/card/preview/repeat query and request counts;</li><li>accessibility behavior;</li><li>tests run and results;</li><li>migrations/reset requirement for this development-only schema change;</li><li>any intentional deviation, tradeoff, or remaining limitation.</li></ul><p>Finish with a concise implementation summary, changed-file list, test evidence, measured budgets, security review findings, and clearly separated follow-ups. Do not report completion while any required variant, authorization control, performance test, Dynamic Table integration, or documentation update remains unfinished.</p>',56)])])}const h=i(r,[["render",n]]);export{m as __pageData,h as default};
