import{_ as i,o as s,c as a,a2 as t}from"./chunks/framework.Buc7gXeZ.js";const k=JSON.parse('{"title":"Embedding Dynamic Tables","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-record-view/embedded-tables.md","filePath":"dynamic-record-view/embedded-tables.md"}'),n={name:"dynamic-record-view/embedded-tables.md"};function o(d,e,l,r,c,h){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="embedding-dynamic-tables" tabindex="-1">Embedding Dynamic Tables <a class="header-anchor" href="#embedding-dynamic-tables" aria-label="Permalink to &quot;Embedding Dynamic Tables&quot;">​</a></h1><p><code>TableContent::table($tableClass)-&gt;relation($name)</code> (set for you by <code>SubApplication::table()-&gt;relation()</code> when building the Other Data section) names an ordinary <code>App\\Livewire\\DynamicTable\\Table</code> subclass and a relation name on the parent model. The relation constraint is enforced <strong>generically, by the framework</strong> — <code>App\\Livewire\\DynamicTable\\Table</code> plus <code>App\\Support\\DynamicRecordView\\Resolution\\EmbeddedTableContext</code> — never by a bespoke subclass hardcoding a foreign key. <strong>The same Table subclass works standalone (unconstrained) and embedded any number of times for different parents (constrained)</strong> — this is the non-negotiable architecture rule for this engine.</p><h2 id="why-one-table-subclass-is-enough-for-every-embedding" tabindex="-1">Why one Table subclass is enough for every embedding <a class="header-anchor" href="#why-one-table-subclass-is-enough-for-every-embedding" aria-label="Permalink to &quot;Why one Table subclass is enough for every embedding&quot;">​</a></h2><p><code>App\\Support\\DynamicTable\\Query\\TableQueryBuilder::query()</code> always starts from <code>($this-&gt;definition-&gt;query)()</code> and only ever layers <code>select()</code>/<code>with()</code>/<code>where()</code>/<code>whereHas()</code> on top — it never resets or replaces the base builder. Search, filters, sort, saved views, and query-string state all flow through <code>TableState::normalize()</code> and then into those same <code>where()</code>/<code>whereHas()</code> calls, so none of them can widen the base query. <code>Table::definition()</code> feeds that base builder through <code>Table::resolvedQuery()</code>, which itself layers the relation constraint on top of the subclass&#39;s own <code>query()</code> — one more layer in the same never-replaced chain, applied generically in the base class, not per subclass.</p><h2 id="the-pattern-modules-general-livewire-applicationstable" tabindex="-1">The pattern (<code>Modules\\General\\Livewire\\ApplicationsTable</code>) <a class="header-anchor" href="#the-pattern-modules-general-livewire-applicationstable" aria-label="Permalink to &quot;The pattern (\`Modules\\General\\Livewire\\ApplicationsTable\`)&quot;">​</a></h2><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ApplicationsTable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Table</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $tableKey </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;general.applications&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Application</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> columns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> filters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>No <code>query()</code> override, no <code>mount()</code> override, no knowledge of being embedded. Used standalone it queries every <code>Application</code>. Embedded — e.g. via <code>SubModuleRecordView</code>:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">SubApplication</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;applications&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    -&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">label</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Applications&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    -&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">table</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ApplicationsTable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    -&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">relation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;applications&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SubModule::applications(): HasMany</span></span></code></pre></div><p>— it&#39;s automatically scoped to the current <code>SubModule</code>. Two different <code>SubModule</code> records embedding the same <code>ApplicationsTable</code> class are two independent, correctly-scoped instances; neither can see the other&#39;s rows.</p><h2 id="how-the-constraint-reaches-the-table-embeddedtablecontext" tabindex="-1">How the constraint reaches the table (<code>EmbeddedTableContext</code>) <a class="header-anchor" href="#how-the-constraint-reaches-the-table-embeddedtablecontext" aria-label="Permalink to &quot;How the constraint reaches the table (\`EmbeddedTableContext\`)&quot;">​</a></h2><p>The browser only ever supplies bounded scalars — never a model, builder, relation, or class name beyond the already-registered <code>Table</code> class. Blade&#39;s <code>x-dynamic-record-view.content</code> component passes exactly these to the embedded <code>@livewire(...)</code> call:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;embedRecordViewKey&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $recordViewKey,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// RecordViewRegistry key</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;embedRecordId&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $record</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;embedSection&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">       =&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;primary&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;other-data&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;embedTab&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">           =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $currentTab</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;embedContent&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">       =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $content</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p><code>Table</code> carries these as <code>#[Locked]</code> public props (immune to client tampering, survive hydration). <code>Table::resolvedQuery()</code> — called fresh from <code>definition()</code> on every request, mount and every subsequent action alike — passes them to <code>EmbeddedTableContext::constrain()</code>, which re-derives everything from the trusted server-side definition on every single call:</p><ol><li>Resolve <code>recordViewKey</code> through <code>RecordViewRegistry</code>.</li><li>Rebuild the <code>DynamicRecordView</code> definition.</li><li>Resolve the parent through <code>RecordResolver::resolveFresh()</code> — a parent deleted or made unauthorized between requests fails safely (404) on the very next embedded-table interaction, not just at initial mount.</li><li>Resolve the section (<code>primarySection()</code>/<code>otherDataSection()</code>), the authorized tab, and the <code>TableContent</code> by key — never by client-supplied index/label.</li><li>Confirm the content is authorized (<code>isVisible($parent)</code>), 404 if not.</li><li>Confirm the requesting <code>Table</code> class exactly matches the class the content block declares (<code>UnknownTableException</code> otherwise).</li><li>Resolve the relation from the parent model by name (<code>UnknownRelationException</code> if it doesn&#39;t exist).</li><li>Confirm it&#39;s a supported relation type — <code>HasMany</code>, <code>BelongsToMany</code>, or <code>MorphMany</code> (<code>UnsupportedRelationTypeException</code> otherwise).</li><li>Confirm the relation&#39;s related model matches the table&#39;s own base-query model (<code>TableModelMismatchException</code> otherwise).</li><li>Layer the constraint on top of the table&#39;s base query via a <code>whereIn</code> subquery against the relation&#39;s own query — never a replacement of the base query.</li></ol><p>If the content block only declared the legacy <code>forRelation()</code> closure (no inspectable <code>relation()</code> name), <code>constrain()</code> returns the base query unconstrained — see &quot;The two relation APIs&quot; below.</p><h2 id="the-two-relation-apis" tabindex="-1">The two relation APIs <a class="header-anchor" href="#the-two-relation-apis" aria-label="Permalink to &quot;The two relation APIs&quot;">​</a></h2><ul><li><strong><code>TableContent::relation(string $name)</code> / <code>SubApplication::relation(string $name)</code> (preferred, canonical)</strong> — a plain, trusted relation name from the PHP definition. Introspectable: <code>EmbeddedTableContext</code> reads it to resolve the actual <code>Relation</code> object, and the Link/Unlink mutation engine (<code>RelationshipMutator</code>) needs exactly this to determine the relation&#39;s name and type. Use this for anything new.</li><li><strong><code>TableContent::forRelation(Closure)</code> / <code>SubApplication::forRelation(Closure)</code> (deprecated, read-only-only)</strong> — a closure <code>fn ($record) =&gt; $record-&gt;applications()</code>. Kept only for backward compatibility. <strong>Not equivalent to <code>relation()</code></strong>: a closure cannot be introspected to recover a relation name, so <code>EmbeddedTableContext</code> does not (and structurally cannot) constrain a query through it — a content block declaring only <code>forRelation()</code> renders its table unconstrained, and Link/Unlink will never support it. Migrate to <code>relation()</code>.</li></ul><p><code>SubModuleRecordView</code> (the canonical example) uses <code>-&gt;relation(&#39;applications&#39;)</code>.</p><h2 id="table-identity-storage-key-vs-instance-key" tabindex="-1">Table identity: storage key vs. instance key <a class="header-anchor" href="#table-identity-storage-key-vs-instance-key" aria-label="Permalink to &quot;Table identity: storage key vs. instance key&quot;">​</a></h2><p><code>Table</code> splits identity into two concepts (previously conflated into a single <code>instanceKey</code>/<code>tableKey()</code>, which incorrectly created one preference/saved-view row per parent record):</p><ul><li><strong><code>storageKey()</code></strong> — returns <code>$this-&gt;tableKey</code>, the plain, permanent identity subclasses declare as a class property (<code>protected string $tableKey = &#39;general.applications&#39;;</code>). Used for <code>TablePreferenceStore</code>/<code>SavedTableViewStore</code>. Never varies by parent, so a preference set while viewing <code>ApplicationsTable</code> embedded under one <code>SubModule</code> applies the same way standalone or under a different <code>SubModule</code>.</li><li><strong><code>instanceIdentifier()</code></strong> — <code>$this-&gt;instanceKey</code> (auto-derived from the <code>embed*</code> props as <code>&quot;{recordViewKey}:{recordId}:{section}:{tab}:{content}&quot;</code>) when embedded, else falls back to <code>storageKey()</code>. Used only for query-string parameter namespacing (<code>queryString()</code>) and the Livewire component&#39;s Blade <code>key(...)</code>. Guarantees two embedded instances of the same table for different parents never share search/filter/sort/page state, and never collide in the URL.</li></ul><p>Standalone tables never set the <code>embed*</code> props, so <code>instanceKey</code> stays blank and <code>instanceIdentifier()</code> just returns <code>storageKey()</code> — identical behavior to before this split.</p><h2 id="per-instance-isolation-and-constant-query-count" tabindex="-1">Per-instance isolation and constant query count <a class="header-anchor" href="#per-instance-isolation-and-constant-query-count" aria-label="Permalink to &quot;Per-instance isolation and constant query count&quot;">​</a></h2><p>Because <code>instanceIdentifier()</code> embeds <code>recordViewKey:parentId:section:tab:content</code>, two embedded instances of the same table class for different parent records never share search, filters, sort, page, or query-string state — each is a fully independent Livewire component instance with its own query-string namespace — while both share the same <code>storageKey()</code>-keyed preferences/saved views. See <code>tests/Feature/DynamicRecordView/SubModuleRecordViewTest.php</code> and <code>tests/Unit/DynamicTable/*</code> for the tests proving this (temporary-state isolation, preference sharing across parents, no per-parent storage row, and non-colliding query-string keys).</p><p>The embedded table&#39;s own query cost doesn&#39;t grow with the number of related rows — see <code>SubModuleRecordViewTest.php</code>&#39;s <code>&#39;keeps a constant query count regardless of related-row count&#39;</code> test.</p><h2 id="link-unlink-mutations" tabindex="-1">Link/Unlink mutations <a class="header-anchor" href="#link-unlink-mutations" aria-label="Permalink to &quot;Link/Unlink mutations&quot;">​</a></h2><p>Built and wired — see <a href="./relationship-actions">relationship-actions.md</a> and <a href="./relation-picker">relation-picker.md</a>.</p><h2 id="second-worked-example-country-→-cities" tabindex="-1">Second worked example: Country → Cities <a class="header-anchor" href="#second-worked-example-country-→-cities" aria-label="Permalink to &quot;Second worked example: Country → Cities&quot;">​</a></h2><p><code>Modules\\General\\System\\World\\CountryRecordView</code> (registered as <code>general.world.country</code>, route <code>general.world.countries.show</code>) embeds the package <code>Nnjeim\\World\\Models\\Country::cities()</code> <code>HasMany</code> relation via a &quot;Cities&quot; tab, using a plain, reusable <code>Modules\\General\\Livewire\\World\\Countries\\CitiesTable</code> (model <code>Nnjeim\\World\\Models\\City</code>) — the same <code>-&gt;relation(&#39;cities&#39;)</code> wiring style as <code>SubModuleRecordView</code>&#39;s Applications tab, and, like that table, no parent-specific <code>query()</code> override, so it works fully unconstrained standalone as well as embedded.</p><p><strong>Link-only, no Unlink — same reasoning as SubModule → Applications.</strong><code>cities.country_id</code> is <code>NOT NULL</code> (see <code>vendor/nnjeim/world/src/Database/Migrations/2020_07_07_055725_create_cities_table.php</code>), so a City can never be &quot;unassigned&quot; and plain Unlink (null out the FK) is architecturally impossible — <code>RelationshipActions::assertSupportedFor()</code> would reject <code>-&gt;unlink()</code> here at definition time. Because no City is ever unassigned, no City is ever a valid Link candidate by default either, <code>allowReassignment()</code> is what makes Link do anything: it lets a candidate already linked to a <em>different</em> Country be relinked to this one, i.e. moving a mis-seeded City to its correct Country — a real, useful administrative correction against a NOT NULL FK, not a fabricated &quot;unassign&quot; capability. See <code>CountryRecordView::subApplications()</code> for the exact configuration and <code>tests/Feature/DynamicRecordView/CountryRecordViewTest.php</code> for the relation isolation, standalone-reuse, Link/reassignment, and flat-query-count coverage.</p>`,30)])])}const y=i(n,[["render",o]]);export{k as __pageData,y as default};
