import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.Buc7gXeZ.js";const u=JSON.parse('{"title":"Saved Views","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-table/saved-views.md","filePath":"dynamic-table/saved-views.md"}'),n={name:"dynamic-table/saved-views.md"};function l(o,e,r,d,p,h){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="saved-views" tabindex="-1">Saved Views <a class="header-anchor" href="#saved-views" aria-label="Permalink to &quot;Saved Views&quot;">​</a></h1><p>Personal, named table configurations — distinct from automatic <a href="./preferences">preferences</a>. A saved view can capture filters, search, sort, column visibility/order, and per-page all at once.</p><h2 id="what-a-view-can-save" tabindex="-1">What a view can save <a class="header-anchor" href="#what-a-view-can-save" aria-label="Permalink to &quot;What a view can save&quot;">​</a></h2><p>Whatever is currently on the Livewire component when <code>saveCurrentView()</code> is called:</p><ul><li><code>search</code></li><li><code>appliedFilters</code></li><li><code>sorts</code></li><li><code>perPage</code></li><li><code>visibleColumns</code></li><li><code>columnOrder</code></li></ul><p><code>Table::rawState()</code> is the exact array captured — the same shape <code>TableState::normalize()</code> consumes everywhere else in the engine, so applying a view reuses the identical security boundary as live query state.</p><h2 id="database-schema" tabindex="-1">Database schema <a class="header-anchor" href="#database-schema" aria-label="Permalink to &quot;Database schema&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>table_views</span></span>
<span class="line"><span>- id</span></span>
<span class="line"><span>- user_id            (FK, cascade on delete)</span></span>
<span class="line"><span>- table_key</span></span>
<span class="line"><span>- name</span></span>
<span class="line"><span>- configuration       JSON — TableState-shaped raw array</span></span>
<span class="line"><span>- schema_version      unsigned int</span></span>
<span class="line"><span>- is_default          boolean</span></span>
<span class="line"><span>- created_at / updated_at</span></span>
<span class="line"><span></span></span>
<span class="line"><span>UNIQUE(user_id, table_key, name)</span></span>
<span class="line"><span>INDEX(user_id, table_key)</span></span></code></pre></div><h2 id="ui-✅-implemented" tabindex="-1">UI (✅ Implemented) <a class="header-anchor" href="#ui-✅-implemented" aria-label="Permalink to &quot;UI (✅ Implemented)&quot;">​</a></h2><p><code>resources/views/components/dynamic-table/saved-views.blade.php</code> renders a &quot;Views&quot; dropdown (shown only when <code>@auth</code>) in the toolbar:</p><ul><li>Lists every saved view; clicking one applies it.</li><li>Shows a &quot;Default&quot; badge next to the default view.</li><li>Shows the currently active view&#39;s name next to the &quot;Views&quot; button label.</li><li>&quot;Set as default&quot; / &quot;Delete&quot; buttons for the active view.</li><li>A &quot;Save current as…&quot; input + button to create/update a view.</li><li>Inline validation error display (e.g. empty name) without a page-level error.</li><li>&quot;Reset to table defaults&quot; clears search/filters/sort/columns back to the table definition&#39;s own defaults and clears the active-view marker — distinct from the column-only preference reset.</li></ul><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Backend actions the UI calls:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$table</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">saveCurrentView</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;My View&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// create, or update in place if the name already exists</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$table</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">applyView</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($viewId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$table</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deleteView</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($viewId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$table</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setDefaultView</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($viewId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$table</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resetToTableDefaults</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// search/filters/sort/columns -&gt; definition defaults</span></span></code></pre></div><ul><li><strong>Create/update (&quot;rename&quot;)</strong>: <code>saveCurrentView(string $name)</code> — <code>EloquentSavedTableViewStore::create()</code> uses <code>updateOrCreate([&#39;user_id&#39;, &#39;table_key&#39;, &#39;name&#39;], [...])</code>, so saving under an existing name updates that view in place rather than creating a duplicate. There is no separate &quot;rename&quot; action — saving the current state under a different name effectively creates a new view instead (renaming-by-resave is the supported pattern, not a distinct rename endpoint).</li><li><strong>Apply</strong>: re-normalizes the stored configuration against the <em>live</em> table definition via <code>TableState::normalize()</code> before overlaying it onto component state, resets pagination to page 1, and sets <code>Table::$activeViewId</code> (display-only — not re-validated against later drift from that view&#39;s original state).</li><li><strong>Delete</strong>: ownership-checked; deleting a view that happens to be the default does <strong>not</strong> auto-promote another view — the table simply falls back to its own <code>defaultSort()</code>/column defaults on the next mount, exactly as if no default view existed. Deleting the active view clears <code>$activeViewId</code>.</li><li><strong>Set default</strong>: exactly one view can be default per user/table — <code>setDefault()</code> clears <code>is_default</code> on every other view for that user/table inside a transaction before setting the target.</li></ul><p>Verified in <code>tests/Feature/DynamicTable/SavedViewsUiTest.php</code>.</p><h2 id="default-views" tabindex="-1">Default views <a class="header-anchor" href="#default-views" aria-label="Permalink to &quot;Default views&quot;">​</a></h2><p>If a user has a default view for a table, <code>Table::mount()</code> automatically calls <code>applyView()</code> on it right after preferences are loaded — the table opens already showing that view. Verified by <code>tests/Feature/DynamicTable/SavedViewsTest.php</code>&#39;s <code>&#39;a default view is applied automatically when the table mounts&#39;</code> test.</p><h2 id="ownership-authorization" tabindex="-1">Ownership &amp; authorization <a class="header-anchor" href="#ownership-authorization" aria-label="Permalink to &quot;Ownership &amp; authorization&quot;">​</a></h2><p>Every <code>SavedTableViewStore</code> method takes the authenticated <code>$user</code> and constrains the query by <code>user_id</code> — a view id belonging to another user is never readable, appliable, deletable, or promotable to default. Attempting any of those operations on someone else&#39;s view id is a silent no-op (see <code>&#39;a user cannot apply delete or default another users saved view&#39;</code>).</p><h2 id="handling-removed-or-unauthorized-columns-filters" tabindex="-1">Handling removed or unauthorized columns/filters <a class="header-anchor" href="#handling-removed-or-unauthorized-columns-filters" aria-label="Permalink to &quot;Handling removed or unauthorized columns/filters&quot;">​</a></h2><p>Because <code>applyView()</code> runs the stored configuration through <code>TableState::normalize()</code> — the exact same function every live request goes through — a saved view referencing a since-removed column, a since-removed filter, or a column whose authorization was revoked is silently repaired: the unknown keys are dropped, not crashed on. See <code>&#39;a stale view referencing a since removed filter is normalized rather than crashing&#39;</code>.</p><h2 id="applying-a-view-resets-pagination" tabindex="-1">Applying a view resets pagination <a class="header-anchor" href="#applying-a-view-resets-pagination" aria-label="Permalink to &quot;Applying a view resets pagination&quot;">​</a></h2><p><code>applyView()</code> always calls <code>Table::resetTablePage()</code> after overlaying state — a saved view never opens on a stale page number.</p><h2 id="shared-views" tabindex="-1">Shared views <a class="header-anchor" href="#shared-views" aria-label="Permalink to &quot;Shared views&quot;">​</a></h2><p>🔮 Planned, not implemented in v1 (per spec). Every view is personal to the user who created it.</p>`,24)])])}const v=a(n,[["render",l]]);export{u as __pageData,v as default};
