import{_ as i,o as r,c as n,a2 as t,j as o,a,t as d}from"./chunks/framework.Buc7gXeZ.js";const b=JSON.parse('{"title":"Security Model","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-table/security.md","filePath":"dynamic-table/security.md"}'),l={name:"dynamic-table/security.md"};function s(c,e,u,h,p,m){return r(),n("div",null,[e[3]||(e[3]=t('<h1 id="security-model" tabindex="-1">Security Model <a class="header-anchor" href="#security-model" aria-label="Permalink to &quot;Security Model&quot;">​</a></h1><h2 id="trust-boundary" tabindex="-1">Trust boundary <a class="header-anchor" href="#trust-boundary" aria-label="Permalink to &quot;Trust boundary&quot;">​</a></h2><p>Everything the browser can influence — Livewire public properties, query-string params, saved view/preference JSON pulled from storage — is <strong>untrusted input</strong>. Only the table definition (<code>columns()</code>, <code>filters()</code>, <code>query()</code>, <code>defaultSort()</code>, all defined in server-side PHP) is trusted.</p><p><code>TableState::normalize(array $raw, TableDefinition $definition): TableState</code> is the <strong>single</strong> point untrusted input must pass through before it can influence a query. <code>TableQueryBuilder</code> only ever accepts a <code>TableState</code>, never a raw array — there is no code path from browser input straight to SQL.</p><h2 id="column-operator-relation-direction-allow-lists" tabindex="-1">Column/operator/relation/direction allow-lists <a class="header-anchor" href="#column-operator-relation-direction-allow-lists" aria-label="Permalink to &quot;Column/operator/relation/direction allow-lists&quot;">​</a></h2><p>Nothing about &quot;which column&quot;, &quot;which operator&quot;, &quot;which relation&quot;, or &quot;which sort direction&quot; is ever taken from client input as a literal SQL identifier:</p><table tabindex="0"><thead><tr><th>Input</th><th>Allow-list enforcement</th></tr></thead><tbody><tr><td>Column key</td><td>Must match a key in <code>TableDefinition::authorizedColumns()</code> (visible + exists)</td></tr><tr><td>Filter key</td><td>Must match a key in <code>TableDefinition::authorizedFilters()</code> (see below)</td></tr><tr><td>Filter operator</td><td>Must be one of the filter&#39;s own <code>TextOperator</code>/<code>NumberOperator</code>/<code>DateOperator</code> cases</td></tr><tr><td>Sort direction</td><td>Must be exactly <code>&#39;asc&#39;</code> or <code>&#39;desc&#39;</code></td></tr><tr><td>Relation path</td><td>Always taken from the <strong>column/filter definition</strong>, never from client input — a <code>RelationColumn</code>/relation filter&#39;s path is baked into the PHP definition</td></tr><tr><td>Enum filter value</td><td>Must be one of <code>$enumClass::cases()</code></td></tr><tr><td>Per-page</td><td>Must be one of <code>TableState::PER_PAGE_OPTIONS</code></td></tr></tbody></table><h2 id="authorization-boundary-visible" tabindex="-1">Authorization boundary: <code>visible()</code> <a class="header-anchor" href="#authorization-boundary-visible" aria-label="Permalink to &quot;Authorization boundary: `visible()`&quot;">​</a></h2><p>An unauthorized column (<code>visible(false)</code>) is excluded from every data path — see <a href="./visibility-authorization">visibility-authorization.md</a> for the full list. This was the subject of a real fix during hardening: <code>TableDefinition::column()</code> is now the single authorization gate every caller (query engine, Livewire component, preference/view normalization) routes through, and <code>TableState</code>&#39;s column-key sources build their allow-list from <code>authorizedColumns()</code> rather than the raw column set. See <code>tests/Feature/DynamicTable/SecurityHardeningTest.php</code>.</p><p><strong>Global search was found to have the same class of gap</strong> and was fixed the same way: <code>TableQueryBuilder::applySearch()</code> previously built its searchable-column list from <code>$this-&gt;definition-&gt;columns</code> (the raw, unauthorized-inclusive set) instead of <code>authorizedColumns()</code>. An unauthorized searchable column&#39;s value was reachable through global search even though the column itself never rendered. Fixed to source from <code>authorizedColumns()</code>; regression tests search for a value that exists <strong>only</strong> behind an unauthorized direct column and an unauthorized relation column and assert zero results, revealing neither the value nor the record&#39;s existence. See <code>tests/Feature/DynamicTable/AuthorizationLeakTest.php</code>.</p><h2 id="filter-authorization" tabindex="-1">Filter authorization <a class="header-anchor" href="#filter-authorization" aria-label="Permalink to &quot;Filter authorization&quot;">​</a></h2><p>Filters have no <code>toggleable()</code>/visibility state of their own by default — <code>Filter::visible()</code> is optional. <code>TableDefinition::filter(string $key)</code> is the single authorization gate every caller (query engine, <code>TableState</code> normalization, filter-panel rendering) routes through, resolved in this order:</p><ol><li><strong>Explicit filter visibility wins.</strong> <code>TextFilter::make(&#39;internal_notes&#39;)-&gt;visible(fn () =&gt; ...)</code> — a filter-only field with no matching column declares its own condition.</li><li><strong>Otherwise, inherit from a same-key column.</strong> <code>TextFilter::make(&#39;credit_limit&#39;)</code> where a <code>credit_limit</code> column also exists — if that column&#39;s <code>visible()</code> is <code>false</code>, the filter is unauthorized too, automatically, even if the column becomes unauthorized after the filter was defined. No separate authorization declaration is needed for the common case.</li><li><strong>Otherwise, default authorized</strong> — matching <code>Column</code>&#39;s own default-visible stance.</li></ol><p>An unauthorized filter (however it becomes unauthorized) is removed from: Livewire state (<code>TableState::normalizeFilters()</code> drops it), the rendered filter panel (<code>authorizedFilters()</code> is what&#39;s passed to the view, never the raw filter set), saved-view application (re-normalized on every apply), and query compilation (<code>TableQueryBuilder::applyFilters()</code> re-checks via <code>definition-&gt;filter()</code>). See <code>tests/Feature/DynamicTable/AuthorizationLeakTest.php</code> for adversarial coverage — including forcing an unauthorized filter directly into raw state and into a saved view&#39;s stored configuration.</p><h2 id="column-manager-and-filter-panel-exposure" tabindex="-1">Column manager and filter panel exposure <a class="header-anchor" href="#column-manager-and-filter-panel-exposure" aria-label="Permalink to &quot;Column manager and filter panel exposure&quot;">​</a></h2><p>Both <code>column-manager.blade.php</code> and <code>filter-panel.blade.php</code> are handed <strong>only</strong><code>$definition-&gt;authorizedColumns()</code> / <code>$definition-&gt;authorizedFilters()</code> by the parent view — never the full definition. An unauthorized column or filter&#39;s label never reaches rendered HTML, not even as a disabled/greyed-out entry.</p><h2 id="state-size-limits" tabindex="-1">State size limits <a class="header-anchor" href="#state-size-limits" aria-label="Permalink to &quot;State size limits&quot;">​</a></h2><p><code>TableState</code> enforces, in addition to <code>MAX_SEARCH_LENGTH</code> and <code>MAX_MULTI_SELECT</code>:</p><ul><li><code>MAX_FILTERS = 20</code> — active filters beyond this are dropped during normalization.</li><li><code>MAX_SORTS = 5</code> — additional sort entries beyond this are dropped; a column appearing twice in a submitted sort list keeps only its first (highest-priority) occurrence.</li><li><code>MAX_PAGE = 100_000</code> — a requested page number above this is clamped, bounding worst-case offset-pagination cost.</li><li>Duplicate entries in <code>visibleColumns</code>/<code>columnOrder</code>/<code>sorts</code> are deduplicated during normalization — <code>array_intersect()</code> on its own preserves duplicates from the untrusted input side, which was a real (non-security-critical, but correctness) gap.</li></ul><p><code>EloquentSavedTableViewStore::create()</code> additionally rejects a configuration payload larger than 20,000 bytes of JSON, so a saved view can&#39;t be abused to store an unbounded blob.</p><h2 id="sql-injection-protection" tabindex="-1">SQL injection protection <a class="header-anchor" href="#sql-injection-protection" aria-label="Permalink to &quot;SQL injection protection&quot;">​</a></h2><ul><li>Every value reaching the database goes through Eloquent&#39;s parameter-bound <code>where()</code>/<code>whereIn()</code>/ <code>whereBetween()</code> — never raw string interpolation or <code>whereRaw()</code>.</li><li>Global search wraps its <code>LIKE</code> value in <code>addcslashes($text, &#39;\\\\%_&#39;)</code> before binding, so a literal <code>%</code>/<code>_</code> in a search term can&#39;t be used to broaden a match.</li><li>Verified with actual injection-shaped payloads (<code>&#39; OR &#39;1&#39;=&#39;1</code>, <code>&#39;; DROP TABLE ...; --</code>, UNION attempts) in <code>SecurityHardeningTest.php</code> — they&#39;re treated as literal search text, never as SQL.</li></ul><h2 id="xss-protection" tabindex="-1">XSS protection <a class="header-anchor" href="#xss-protection" aria-label="Permalink to &quot;XSS protection&quot;">​</a></h2>',23)),o("ul",null,[o("li",null,[e[0]||(e[0]=a("Blade's ",-1)),o("code",null,d(),1),e[1]||(e[1]=a(" auto-escapes all rendered column values by default.",-1))]),e[2]||(e[2]=t("<li>No <code>HtmlColumn</code> or raw-HTML rendering path exists in this version (❌ Unsupported by design, per spec — would need to be an explicit, clearly-labeled trusted column type if ever added).</li><li><code>Column::getLink()</code> validates the URL scheme server-side: only <code>http</code>/<code>https</code> (or relative) schemes are returned; a <code>javascript:</code> (or any other) scheme from a <code>link()</code> callback is discarded before it ever reaches the view.</li>",2))]),e[4]||(e[4]=t('<h2 id="authorization-scopes" tabindex="-1">Authorization scopes <a class="header-anchor" href="#authorization-scopes" aria-label="Permalink to &quot;Authorization scopes&quot;">​</a></h2><p><code>query()</code> is the one place tenant/company/permission scoping belongs — every generated query starts from it and the query engine only ever <em>adds</em> <code>where</code>/<code>whereHas</code>/<code>orderBy</code> clauses on top, never replaces or bypasses the base query. The global-search OR-group is nested inside a single <code>where(function ...)</code> specifically so it can never widen the base scope via short-circuit OR logic.</p><h2 id="preference-saved-view-ownership" tabindex="-1">Preference &amp; saved-view ownership <a class="header-anchor" href="#preference-saved-view-ownership" aria-label="Permalink to &quot;Preference &amp; saved-view ownership&quot;">​</a></h2><p>Both <code>TablePreferenceStore</code> and <code>SavedTableViewStore</code> implementations constrain every query by the given <code>$user</code>&#39;s own id — there is no method signature that accepts a raw user id from the client. Cross-user access attempts (loading, saving, deleting, or promoting another user&#39;s preferences or views) are silent no-ops, verified in <code>PreferencesTest.php</code> and <code>SavedViewsTest.php</code>.</p><h2 id="export-security" tabindex="-1">Export security <a class="header-anchor" href="#export-security" aria-label="Permalink to &quot;Export security&quot;">​</a></h2><p>Not yet implemented (<code>exportable()</code> is a flag only — see <a href="./columns">columns.md</a>). When built, it must reuse the exact same authorized <code>TableDefinition</code>/<code>TableState</code> path as the live table — never a parallel, unscoped export query.</p><h2 id="size-rate-limits" tabindex="-1">Size/rate limits <a class="header-anchor" href="#size-rate-limits" aria-label="Permalink to &quot;Size/rate limits&quot;">​</a></h2><ul><li>Search text: <code>TableState::MAX_SEARCH_LENGTH = 200</code></li><li>Multi-select (enum/BelongsTo filters): <code>TableState::MAX_MULTI_SELECT = 50</code></li><li>Per-page: allowlisted, <code>TableState::PER_PAGE_OPTIONS</code></li><li>Saved view name: truncated to 100 chars, empty name rejected (<code>ValidationException</code>)</li></ul><h2 id="fail-safe-on-malformed-state" tabindex="-1">Fail-safe on malformed state <a class="header-anchor" href="#fail-safe-on-malformed-state" aria-label="Permalink to &quot;Fail-safe on malformed state&quot;">​</a></h2><p>Every <code>normalize*</code> helper in <code>TableState</code> degrades gracefully: an unrecognized column, filter, operator, direction, or value is dropped, not thrown as an error to the user. The only exceptions thrown are <strong>developer-configuration</strong> errors (duplicate keys, invalid enum class, etc.) at <code>TableDefinition</code>/column-definition time — never from live client input. No SQL error or stack trace is ever surfaced to the browser from tampered state.</p>',10))])}const y=i(l,[["render",s]]);export{b as __pageData,y as default};
