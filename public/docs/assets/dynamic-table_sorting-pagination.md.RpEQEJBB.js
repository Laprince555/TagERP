import{_ as i,o as a,c as s,a2 as t}from"./chunks/framework.Buc7gXeZ.js";const g=JSON.parse('{"title":"Sorting & Pagination","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-table/sorting-pagination.md","filePath":"dynamic-table/sorting-pagination.md"}'),n={name:"dynamic-table/sorting-pagination.md"};function o(r,e,l,d,p,c){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="sorting-pagination" tabindex="-1">Sorting &amp; Pagination <a class="header-anchor" href="#sorting-pagination" aria-label="Permalink to &quot;Sorting &amp; Pagination&quot;">​</a></h1><h2 id="default-sorting" tabindex="-1">Default sorting <a class="header-anchor" href="#default-sorting" aria-label="Permalink to &quot;Default sorting&quot;">​</a></h2><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultSort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> array</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Sort</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;created_at&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">descending</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>Sort::make($column)-&gt;ascending()</code> / <code>-&gt;descending()</code> build the <code>sorts</code> array applied on first mount. Only ever set via the table definition — never client input.</p><h2 id="single-multi-column-sorting-✅-implemented-including-the-ui" tabindex="-1">Single &amp; multi-column sorting (✅ Implemented, including the UI) <a class="header-anchor" href="#single-multi-column-sorting-✅-implemented-including-the-ui" aria-label="Permalink to &quot;Single &amp; multi-column sorting (✅ Implemented, including the UI)&quot;">​</a></h2><ul><li><strong>Single sort</strong>: click a header — <code>Table::sortBy(string $column)</code> replaces the entire sort list. Clicking the same header again flips ascending/descending.</li><li><strong>Additive multi-sort</strong>: shift-click a header — <code>Table::sortByAdditive(string $column)</code> appends it as the next sort priority instead of replacing the list (or flips its direction in place if it&#39;s already sorted). Capped at <code>TableState::MAX_SORTS = 5</code>. A priority badge (<code>1</code>, <code>2</code>, ...) renders next to each header once more than one column is sorted.</li><li><strong>Remove one sort</strong>: <code>Table::removeSort(string $column)</code>.</li><li><strong>Reset to default</strong>: <code>Table::resetSort()</code> restores <code>defaultSort()</code>.</li></ul><p><code>TableState::normalizeSorts()</code> accepts and validates any number of <code>{column, direction}</code> pairs (deduplicated per column, capped at <code>MAX_SORTS</code>) — this is the same security boundary regardless of whether the UI produced single or multi-sort state.</p><p>Only columns with <code>-&gt;sortable()</code> set are ever accepted:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">protected</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> normalizeSorts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> is_string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($column) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $definition</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">column</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($column)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isSortable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        continue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// dropped</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> in_array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($direction, [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;asc&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;desc&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        continue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// dropped</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>$definition-&gt;column($column)</code> also enforces authorization — an unauthorized column can never be sorted even if it happens to be <code>sortable()</code> (see <a href="./visibility-authorization">visibility-authorization.md</a>).</p><h2 id="stable-sorting-primary-key-tie-breaker" tabindex="-1">Stable sorting (primary-key tie-breaker) <a class="header-anchor" href="#stable-sorting-primary-key-tie-breaker" aria-label="Permalink to &quot;Stable sorting (primary-key tie-breaker)&quot;">​</a></h2><p><code>TableQueryBuilder::applySort()</code> always appends <code>orderBy($model-&gt;getQualifiedKeyName(), &#39;asc&#39;)</code> after any user-requested sorts — so two rows with an identical sort value always come back in the same order across pages, instead of shuffling between requests.</p><h2 id="column-ordering-✅-implemented-including-drag-and-drop" tabindex="-1">Column ordering (✅ Implemented, including drag-and-drop) <a class="header-anchor" href="#column-ordering-✅-implemented-including-drag-and-drop" aria-label="Permalink to &quot;Column ordering (✅ Implemented, including drag-and-drop)&quot;">​</a></h2><p><code>columnOrder</code> is a separate concern from row sorting — it controls which order columns render in, not which order rows come back in. Default order is the order returned by <code>columns()</code>.</p><ul><li><strong>Drag-and-drop</strong>: the column manager renders toggleable columns inside a Livewire 4 <code>wire:sort</code> list. <code>Table::sortColumns(string $item, int $position)</code> is the <code>wire:sort</code> handler — fires once per completed drop (SortableJS only calls <code>onSort</code> on drag end, not per pointer move), and delegates to <code>reorderColumns()</code>&#39;s authorization-safe merge.</li><li><strong>Keyboard-accessible fallback</strong>: Up/Down buttons next to each toggleable column call the same <code>sortColumns()</code> action, for users who can&#39;t drag-and-drop.</li><li>Fixed (<code>toggleable(false)</code>) columns are never draggable and always kept in their defined relative position — enforced in <code>reorderColumns()</code>, not just the UI.</li><li><code>Table::reorderColumns(array $order)</code> persists the result as one preference write via <code>TablePreferenceStore</code> — see <a href="./preferences">preferences.md</a>.</li></ul><h2 id="pagination-modes-✅-implemented-standard-simple-cursor-unimplemented" tabindex="-1">Pagination modes (✅ Implemented: standard, simple; cursor unimplemented) <a class="header-anchor" href="#pagination-modes-✅-implemented-standard-simple-cursor-unimplemented" aria-label="Permalink to &quot;Pagination modes (✅ Implemented: standard, simple; cursor unimplemented)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Mode</th><th>How to select it</th><th>Count query?</th></tr></thead><tbody><tr><td><strong>Standard</strong> (length-aware)</td><td>Default — no override needed</td><td>✅ Exactly one <code>count(*)</code></td></tr><tr><td><strong>Simple</strong></td><td><code>protected function paginationMode(): string { return &#39;simple&#39;; }</code></td><td>❌ None — cheaper for large tables where &quot;total pages&quot; isn&#39;t needed</td></tr><tr><td><strong>Cursor</strong></td><td>Requesting <code>&#39;cursor&#39;</code> throws <code>InvalidArgumentException</code> immediately</td><td>❌ Unimplemented (explicitly out of scope; the spec allows deferring it) — never silently falls back to another mode</td></tr></tbody></table><p><code>Table::render()</code> calls <code>paginate()</code> or <code>simplePaginate()</code> based on <code>paginationMode()</code>. The pagination view (<code>resources/views/components/dynamic-table/pagination.blade.php</code>) checks <code>$paginator instanceof LengthAwarePaginator</code> before calling <code>total()</code>/<code>firstItem()</code>/<code>lastItem()</code> — none of those exist on a plain <code>Paginator</code> (simple mode), and calling them throws <code>BadMethodCallException</code>. A simple-mode table shows &quot;Page N&quot; instead of &quot;X–Y of Z&quot;.</p><p>Verified by <code>tests/Feature/DynamicTable/PerformanceRegressionTest.php</code> (query-count) and <code>tests/Feature/DynamicTable/PaginationModeTest.php</code> (mode selection, simple-mode rendering never calls <code>total()</code>, invalid mode throws rather than silently falling back).</p><h2 id="per-page" tabindex="-1">Per-page <a class="header-anchor" href="#per-page" aria-label="Permalink to &quot;Per-page&quot;">​</a></h2><p><code>TableState::PER_PAGE_OPTIONS = [10, 25, 50, 100]</code>, default <code>25</code>. Any value outside this allowlist is rejected and falls back to the default — never trusted as raw client input.</p><h2 id="pagination-reset-behavior" tabindex="-1">Pagination reset behavior <a class="header-anchor" href="#pagination-reset-behavior" aria-label="Permalink to &quot;Pagination reset behavior&quot;">​</a></h2><p><code>page</code> resets to <code>1</code> whenever:</p><ul><li>Search is submitted (<code>submitSearch()</code>)</li><li>Filters are applied or cleared (<code>applyFilters()</code>, <code>clearFilters()</code>, <code>clearFilter()</code>)</li><li>Sort changes (<code>sortBy()</code>)</li><li>Per-page changes (<code>setPerPage()</code>)</li><li>A saved view is applied (<code>applyView()</code>)</li><li>Preferences are reset (<code>resetPreferences()</code>)</li></ul><p>This prevents landing on a now-nonexistent page (e.g. page 5 of a filtered set that only has 2 pages).</p>`,25)])])}const k=i(n,[["render",o]]);export{g as __pageData,k as default};
