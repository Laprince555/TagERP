import{_ as o,o as a,c as s,a2 as r,j as i,a as t,t as n}from"./chunks/framework.Buc7gXeZ.js";const y=JSON.parse('{"title":"Record References","description":"","frontmatter":{},"headers":[],"relativePath":"record-references/index.md","filePath":"record-references/index.md"}'),d={name:"record-references/index.md"};function l(c,e,p,h,u,g){return a(),s("div",null,[e[6]||(e[6]=r('<h1 id="record-references" tabindex="-1">Record References <a class="header-anchor" href="#record-references" aria-label="Permalink to &quot;Record References&quot;">​</a></h1><p>One shared way to present any Application record — a Country, a Company, an SubModule, etc. — as a <code>card</code>, <code>tag</code>, or <code>icon</code>. Every variant is driven by the same Application-owned provider; nothing about title/facts/route/authorization is ever duplicated in a view, a Dynamic Table column, or a future Dynamic Form field.</p><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Vertical slice complete for <strong>Country</strong> (<code>gen-wld-ctr</code>, backed by <code>nnjeim/world</code>&#39;s <code>Nnjeim\\World\\Models\\Country</code>). Only this Application has a registered provider today — see &quot;Rolling out to another Application&quot; below to add the next one.</p><h2 id="core-namespaces-app-support-recordreference" tabindex="-1">Core namespaces (<code>app/Support/RecordReference/</code>) <a class="header-anchor" href="#core-namespaces-app-support-recordreference" aria-label="Permalink to &quot;Core namespaces (`app/Support/RecordReference/`)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Class</th><th>Purpose</th></tr></thead><tbody><tr><td><code>RecordReferenceVariant</code></td><td><code>Card</code> | <code>Tag</code> | <code>Icon</code> enum</td></tr><tr><td><code>ApplicationColor</code></td><td>Fixed 9-color palette allowlist (<code>applications.color</code> column)</td></tr><tr><td><code>RecordReferencePalette</code></td><td>Resolves a color token to trusted Tailwind classes — never interpolates</td></tr><tr><td><code>RecordFact</code></td><td>One escaped label/value/priority triple</td></tr><tr><td><code>RecordReferenceIdentity</code></td><td>Minimal initial payload (icon, title, url) — zero preview data</td></tr><tr><td><code>RecordReferencePreview</code></td><td>On-demand facts payload, <code>available: false</code> on any denial</td></tr><tr><td><code>RecordReferenceProvider</code></td><td>Interface each Application implements</td></tr><tr><td><code>RecordReferenceRegistry</code></td><td>Application code → provider map, populated by module <code>boot()</code></td></tr><tr><td><code>RecordReferenceResolver</code></td><td>Builds DTOs from an already-loaded record; never queries</td></tr></tbody></table><p>Blade: <code>resources/views/components/record-reference/{card,tag,icon}.blade.php</code> (stateless). Preview host: <code>app/Livewire/RecordReference/PreviewHost.php</code> + its Blade view, mounted <strong>once</strong> in <code>resources/views/layouts/app.blade.php</code>.</p><h2 id="registering-a-provider" tabindex="-1">Registering a provider <a class="header-anchor" href="#registering-a-provider" aria-label="Permalink to &quot;Registering a provider&quot;">​</a></h2><ol><li>Implement <code>RecordReferenceProvider</code> inside the owning module (see <code>Modules/General/System/World/CountryRecordReferenceProvider.php</code>).</li><li>Register it in that module&#39;s <code>ServiceProvider::boot()</code>:<div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RecordReferenceRegistry</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> CountryRecordReferenceProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div></li><li>Make sure the Application has a real, authorized, named record route the provider&#39;s <code>url()</code> can call (<code>route()</code>, never string concatenation). Country&#39;s route (<code>general.world.countries.show</code>) didn&#39;t exist before this feature — see <code>Modules/General/Livewire/World/Countries/CountryRecordView.php</code>, which reuses the existing Dynamic Record View engine (<code>App\\Support\\DynamicRecordView</code>) rather than a bespoke controller.</li></ol><h2 id="rendering" tabindex="-1">Rendering <a class="header-anchor" href="#rendering" aria-label="Permalink to &quot;Rendering&quot;">​</a></h2><div class="language-blade vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">blade</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">x-record-reference.card</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> :identity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$identity&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> :facts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$facts&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">x-record-reference.tag</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> :identity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$identity&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">x-record-reference.icon</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> :identity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$identity&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div><p>Build <code>$identity</code>/<code>$facts</code> via <code>RecordReferenceResolver::identity()</code> / <code>::cardFacts()</code> from a record you already loaded — never inside the Blade component.</p><h2 id="lazy-preview-lifecycle" tabindex="-1">Lazy preview lifecycle <a class="header-anchor" href="#lazy-preview-lifecycle" aria-label="Permalink to &quot;Lazy preview lifecycle&quot;">​</a></h2><ul><li><strong>Card</strong>: all facts are fetched in the same bounded query that loaded the visible rows. No secondary request, ever.</li><li><strong>Tag</strong>: identity only on initial render. Right-click, <code>Shift+F10</code>/Context-Menu key, or the touch &quot;...&quot; button dispatches a <code>record-reference:open-preview</code> window <code>CustomEvent</code>.</li><li><strong>Icon</strong>: identity only on initial render. Focus, touch, or a 400 ms stable hover dispatches the same event (hover is cancelled via <code>clearTimeout</code> if the pointer leaves first).</li><li>The single <code>PreviewHost</code> Livewire component listens for that event, resolves the provider via the server registry, authorizes, and queries <strong>only</strong> <code>identityColumns() + previewColumns()</code>.</li><li>The client (Alpine, inline in <code>preview-host.blade.php</code>) keeps a per-page <code>cache</code> object keyed by <code>code:key</code> and an <code>inflight</code> map to dedupe concurrent triggers. Reopening the same record in the same page load never issues a second Livewire call.</li><li>Cross-request fact caching is off by default (<code>provider-&gt;cacheTtl(): null</code>). Application metadata is memoized per Dynamic Table render (one query for every distinct Application code referenced, not one per row) — no dedicated cross-request cache layer was added; wiring into the existing navigation cache is a follow-up (see Limitations).</li></ul><h2 id="security-boundaries" tabindex="-1">Security boundaries <a class="header-anchor" href="#security-boundaries" aria-label="Permalink to &quot;Security boundaries&quot;">​</a></h2>',15)),i("ul",null,[e[4]||(e[4]=r("<li>Client input to <code>PreviewHost::loadPreview()</code> is two bounded scalar strings (<code>applicationCode</code>, <code>recordKey</code>) — never a class, route, column, or SQL fragment.</li><li><code>PreviewHost</code> always resolves through <code>RecordReferenceRegistry</code> — a forged/unknown code returns the same generic <code>{available: false}</code> shape as an unauthorized or missing record. No existence leak.</li><li><code>authorize()</code> is provider-owned and re-checked on every call; nothing is cached across users.</li>",3)),i("li",null,[e[0]||(e[0]=t("Facts are plain scalars rendered through Blade's ",-1)),i("code",null,n(),1),e[1]||(e[1]=t(" escaping — a hostile title/fact value renders as literal text, never HTML (see ",-1)),e[2]||(e[2]=i("code",null,"RecordReferenceBladeVariantsTest",-1)),e[3]||(e[3]=t(").",-1))]),e[5]||(e[5]=i("li",null,[i("code",null,"RecordReferenceColumn::getLink()"),t("/"),i("code",null,"Application::url()"),t(" only ever produce "),i("code",null,"route()"),t("-generated URLs.")],-1))]),e[7]||(e[7]=r('<h2 id="measured-budgets-see-test-evidence-in-the-three-doc-files-pr-report" tabindex="-1">Measured budgets (see test evidence in the three doc files + PR report) <a class="header-anchor" href="#measured-budgets-see-test-evidence-in-the-three-doc-files-pr-report" aria-label="Permalink to &quot;Measured budgets (see test evidence in the three doc files + PR report)&quot;">​</a></h2><ul><li>Icon/tag Dynamic Table render: 1 count query + 1 data query, 0 preview queries, constant regardless of row count (<code>DynamicTableRecordReferenceColumnTest</code>).</li><li>Card render: identity + declared card columns only, in the same select — 0 secondary queries.</li><li>First preview: 1 primary record query (0 relation queries for Country since it has none declared).</li><li>Repeat preview open in the same page load: 0 requests (client cache; not exercised by Pest, which cannot assert &quot;no network request&quot; — see Limitations).</li></ul><h2 id="known-limitations-deviations" tabindex="-1">Known limitations / deviations <a class="header-anchor" href="#known-limitations-deviations" aria-label="Permalink to &quot;Known limitations / deviations&quot;">​</a></h2><ul><li>Only Country has a provider. Rolling out State/City/Timezone/Currency/Language/Company/Person is explicitly out of scope for this slice (per the original instruction to prove the vertical slice first).</li><li>Application metadata memoization is request-scoped only (per Dynamic Table render / per <code>PreviewHost</code> call), not wired into a cross-request cache tag. <code>RecordReferenceRegistry</code> is bound <code>scoped()</code> for the same reason — safe default, not a performance ceiling.</li><li>The &quot;zero HTTP request on reopen&quot; guarantee lives entirely in the Alpine client cache in <code>preview-host.blade.php</code>; there is no Pest browser test proving it (no Pest browser testing package is configured in this project — confirmed before writing tests). It is covered by reading the client code + unit-level <code>PreviewHost::loadPreview()</code> correctness instead.</li><li>Popover collision-aware repositioning is viewport-collision-aware: it implements horizontal clamping, vertical flipping when exceeding the viewport height, and scroll/resize repositioning.</li></ul>',4))])}const v=o(d,[["render",l]]);export{y as __pageData,v as default};
