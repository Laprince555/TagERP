import{_ as o,o as t,c as i,a2 as s}from"./chunks/framework.Buc7gXeZ.js";const u=JSON.parse('{"title":"Record Resolution","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic-record-view/record-resolution.md","filePath":"dynamic-record-view/record-resolution.md"}'),a={name:"dynamic-record-view/record-resolution.md"};function r(d,e,n,c,l,h){return t(),i("div",null,[...e[0]||(e[0]=[s('<h1 id="record-resolution" tabindex="-1">Record Resolution <a class="header-anchor" href="#record-resolution" aria-label="Permalink to &quot;Record Resolution&quot;">​</a></h1><p><code>App\\Support\\DynamicRecordView\\Resolution\\RecordResolver::resolve($view, $id)</code> is the only way a <code>DynamicRecordView</code>&#39;s record is ever loaded:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $view</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">find</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($id);</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">abort_if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">404</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><ul><li><strong>Never</strong> <code>Model::findOrFail()</code> — always through the developer&#39;s own <code>query()</code>, so any scope, soft-delete exclusion, or explicit authorization <code>where()</code> baked into <code>query()</code> is respected.</li><li>A missing row and a row excluded by <code>query()</code> (i.e. &quot;exists but you&#39;re not authorized&quot;) both 404 identically — a tampered id can&#39;t distinguish the two.</li><li><strong>Memoized per request</strong>, keyed by <code>$view::class.&#39;::&#39;.$id</code>, bound as <code>$this-&gt;app-&gt;scoped(RecordResolver::class)</code> in <code>AppServiceProvider</code> — so <code>mount()</code> and <code>render()</code> calling <code>resolveRecord()</code> multiple times in one Livewire request never re-query.</li></ul><p><code>App\\Livewire\\DynamicRecordView\\RecordView::resolveRecord()</code> and <code>OtherData::resolveRecord()</code> are the adapters that call this from a Livewire component. See <code>tests/Feature/DynamicRecordView/RecordResolutionTest.php</code> for the security tests (tampered id, unauthorized query, memoization).</p><h2 id="resolvefresh-—-action-entry-points" tabindex="-1"><code>resolveFresh()</code> — action entry points <a class="header-anchor" href="#resolvefresh-—-action-entry-points" aria-label="Permalink to &quot;`resolveFresh()` — action entry points&quot;">​</a></h2><p>Per-request memoization means a record deleted or made unauthorized <em>after</em><code>mount()</code> would otherwise still resolve from cache on a later action within the same request/instance. <code>RecordResolver::resolveFresh($view, $id)</code> drops the memoized entry (if any) and resolves again, so it always reflects current state. Every Livewire component action that (re)touches the record — <code>RecordView::setActiveTab()</code> and <code>OtherData::setActiveTab()</code> — calls <code>resolveFresh()</code>, not <code>resolve()</code>, so a parent deleted or unauthorized between mount and the next action 404s on that action too, not just on the next full page load. <code>OtherData::resolveRecord()</code> itself is also never cached on the component (it re-resolves via the resolver on every call), matching <code>RecordView</code>&#39;s pattern, so authorization callbacks always receive the actual current model, not a stale one keyed only off the raw id.</p><p>Note: because Livewire converts <code>HttpException</code> (including the <code>NotFoundHttpException</code> <code>abort_if(...)</code> throws) into a normal 404 HTTP response rather than letting it bubble up as a PHP exception (see <code>Livewire\\Features\\SupportTesting\\RequestBroker::temporarilyDisableExceptionHandlingAndMiddleware()</code>), tests assert <code>-&gt;assertStatus(404)</code> on the Livewire test instance rather than <code>expect(...)-&gt;toThrow(NotFoundHttpException::class)</code>.</p>',8)])])}const k=o(a,[["render",r]]);export{u as __pageData,k as default};
